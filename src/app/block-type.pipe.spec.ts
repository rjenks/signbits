import { BlockTypePipe } from './block-type.pipe';

describe('BlockTypePipe', () => {
  it('create an instance', () => {
    const pipe = new BlockTypePipe();
    expect(pipe).toBeTruthy();
  });
});
